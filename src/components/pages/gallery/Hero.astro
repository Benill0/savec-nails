<section class="gallery-hero">
  <div class="container">
    <div class="content">
      <span class="badge">📸 Galería</span>
      <h1 class="title">Nuestro Arte</h1>
      <p class="description">
        Descubre algunos de nuestros trabajos y el detalle que nos caracteriza.
        Cada diseño refleja técnica, dedicación y pasión por la belleza.
      </p>
    </div>
  </div>
</section>

<style>
  .gallery-hero {
    background-color: var(--background);
    padding: 8rem 1.5rem;
    text-align: center;
  }

  .container {
    max-width: var(--max-width);
    margin: 0 auto;
  }

  .content {
    max-width: 36rem; /* max-w-xl */
    margin: 0 auto;
  }

  .badge {
    display: inline-block;
    font-size: 0.875rem; /* text-sm */
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: var(--primary);
    margin-bottom: 0.75rem; /* mb-3 */
    font-family: var(--font-body);
  }

  .title {
    font-family: var(--font-display);
    font-size: clamp(2.5rem, 5vw, 3.5rem); /* text-4xl md:text-5xl */
    font-weight: 700;
    color: var(--text-main);
    margin-bottom: 1rem; /* mb-4 */
    line-height: 1.2;
  }

  .description {
    font-family: var(--font-body);
    font-size: 1.125rem;
    color: var(--muted-text);
    line-height: 1.625; /* leading-relaxed */
  }

  /* Ajuste por si el Header es Fixed */
  @media (max-width: 768px) {
    .gallery-hero {
      padding: 6rem 1.5rem;
    }
  }
</style>
